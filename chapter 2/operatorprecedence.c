#include <stdio.h>

int main(){
    int a=4,b=5;
    printf("the result is %d", a*4/b*8);
    // thus it is proved that first we calculate a*4/b and then the result iss multiplied by 8
    return 0;
}