#include <stdio.h>
// if else and switch statement

int main(){
   


    // printf("WELCOME,\nTHIS IS A PROGRAM TO CALCULATE IF A NUMBER IS DIVISIBLE BY 97\n");
    // int a;
    // printf("enter the number to be calculated about");
    // scanf("%d",&a);
    // if (a%97==0)
    // {
    //     printf("yes the no is divisible by 97");
    // }
    // else{
    //     printf("no the no is not divisible");
    // }
    
    // && is used for evaluating and operations
    // || is used for evaluating or operations
    // ! not operator
    // we can use conditional operator too
    // like here
    // condition? if true: if false
    int a;
    printf("enter a number");
    scanf("%d",&a);
    (a>10)? printf("a is greater than 10"):printf("a is lesser than 10");
    return 0;
}