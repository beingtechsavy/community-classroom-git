#include <stdio.h>
int main(){
    float l,b;
    printf("enter the length");
    scanf("%f",&l);
    printf("enter the breadth");
    scanf("%f",&b);
    float area=l*b;
    printf("the area is %f", area);
    return 0;
}